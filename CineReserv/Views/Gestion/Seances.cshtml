@model IEnumerable<CineReserv.Models.Seance>

@{
    ViewData["Title"] = "Gestion des Séances";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>🎫 Gestion des Séances</h1>
        <a asp-action="CreateSeance" class="btn btn-success">
            <i class="fas fa-plus"></i> Ajouter une séance
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        @foreach (var seance in Model)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@seance.Film.Titre</h5>
                        <p class="card-text">
                            <strong>📅 Date :</strong> @seance.DateHeure.ToString("dd/MM/yyyy HH:mm")<br>
                            <strong>🏢 Salle :</strong> @seance.Salle.Nom<br>
                            <strong>🪑 Places :</strong> @seance.Salle.NombrePlaces<br>
                            <strong>🎬 Genre :</strong> @seance.Film.Genre
                        </p>
                        <div class="mt-auto">
                            <div class="d-flex gap-2">
                                <a asp-action="EditSeance" asp-route-id="@seance.Id" class="btn btn-outline-primary btn-sm">
                                    <i class="fas fa-edit"></i> Modifier
                                </a>
                                <a asp-action="DeleteSeance" asp-route-id="@seance.Id" class="btn btn-outline-danger btn-sm">
                                    <i class="fas fa-trash"></i> Supprimer
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info text-center">
            <h4>Aucune séance trouvée</h4>
            <p>Commencez par ajouter votre première séance.</p>
            <a asp-action="CreateSeance" class="btn btn-primary">
                <i class="fas fa-plus"></i> Ajouter une séance
            </a>
        </div>
    }
</div>


