@model CineReserv.Models.Seance

@{
    ViewData["Title"] = "Modifier la Séance";
}

<div class="container mt-4">
    <h1>🎫 Modifier la Séance</h1>

    <div class="row">
        <div class="col-md-8">
            <form asp-action="EditSeance" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="FilmId" class="form-label">Film</label>
                    <select asp-for="FilmId" class="form-select" asp-items="@(new SelectList(ViewBag.Films, "Id", "Titre"))">
                        <option value="">Choisir un film</option>
                    </select>
                    <span asp-validation-for="FilmId" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="SalleId" class="form-label">Salle</label>
                    <select asp-for="SalleId" class="form-select" asp-items="@(new SelectList(ViewBag.Salles, "Id", "Nom"))">
                        <option value="">Choisir une salle</option>
                    </select>
                    <span asp-validation-for="SalleId" class="text-danger"></span>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="DateHeure" class="form-label">Date et heure</label>
                        <input asp-for="DateHeure" class="form-control" type="datetime-local" />
                        <span asp-validation-for="DateHeure" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="EstActive" class="form-label">Statut</label>
                        <select asp-for="EstActive" class="form-select">
                            <option value="true">Active</option>
                            <option value="false">Inactive</option>
                        </select>
                        <span asp-validation-for="EstActive" class="text-danger"></span>
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Enregistrer les modifications
                    </button>
                    <a asp-action="Seances" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Retour à la liste
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>


